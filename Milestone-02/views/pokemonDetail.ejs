<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pokémon Detail - <%= pokemon.name %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('partials/navbar') %>
  <div class="container">
    <h1>Detail for <%= pokemon.name %> (<%= pokemon.id %>)</h1>
    
    <img src="<%= pokemon.imageUrl %>" alt="<%= pokemon.name %>" width="250">
    <p><strong>Description:</strong> <%= pokemon.description %></p>
    <p><strong>Age:</strong> <%= pokemon.age %></p>
    <p><strong>Active:</strong> <%= pokemon.isActive %></p>
    <p><strong>Birthdate:</strong> <%= pokemon.birthDate %></p>
    <p><strong>Rarity:</strong> <%= pokemon.rarity %></p>
    <p><strong>Abilities:</strong> <%= pokemon.abilities.join(', ') %></p>
    
    <h2>Trainer Information</h2>
    <% if (trainer) { %>
      <p><strong>Name:</strong> <%= trainer.name %></p>
      <p><strong>Region:</strong> <%= trainer.region %></p>
      <img src="<%= trainer.imageUrl %>" alt="<%= trainer.name %>" width="150">
      <p><a href="/trainer/<%= trainer.id %>">View Full Trainer Details</a></p>
    <% } else { %>
      <p>No trainer information available.</p>
    <% } %>
    
    <a href="/pokemon">Back to Pokémon Overview</a>
  </div>
</body>
</html>
